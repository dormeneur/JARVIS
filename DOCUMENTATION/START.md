# ✅ Prompt for AI Agent

You are my software engineering agent.

I’m building a **local-first AI professional life manager** (Flutter mobile app + laptop-hosted FastAPI server + local LLM via Ollama). All user data lives locally on my laptop in a single encrypted database and syncs securely to my phone after device pairing (QR + keys). The app manages **resume version history (Git-like) using LaTeX**, supports **JD → tailored resume branches/categories**, and includes an **AI writer** for bios, internship form answers, LinkedIn/X drafts, etc — **with strict “never invent facts” rules**.

I’m attaching the PRD + system design documentation below.

**Step 1:** Read the PRD fully and summarize your understanding in your own words.
**Step 2:** Ask me any clarifying questions you need *before building anything*.
**Step 3:** After I answer, generate a complete **Agent Build Doc** including: folder structure, DB schema/migrations, API contract, sync logic, resume compile pipeline, prompt-injection design (`best-practices.md` + `user-preference.md`), and MVP milestones with acceptance criteria.

Keep the plan simple and implementable, focusing only on MVP.

(Attached PRD here: B:\DEV\SECOND-BRAIN\PRD.md)
