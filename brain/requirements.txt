fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic-settings==2.8.1
httpx==0.28.1

# Document processing
# 1.25.3 ships no cp313 wheel, so the brain image failed to build on the
# python:3.13 base. 1.26.x is the first line with 3.13 wheels.
PyMuPDF==1.26.7
python-docx==1.1.2

# Text chunking — langchain-text-splitters is the minimal package that
# provides RecursiveCharacterTextSplitter. The full langchain package is
# not needed and pulls in ~100 transitive deps including sqlalchemy.
tiktoken==0.9.0
langchain-text-splitters==0.3.8

# Testing
pytest==8.3.4
hypothesis==6.122.3

# Utilities
tenacity==9.0.0
chromadb-client==0.5.23
